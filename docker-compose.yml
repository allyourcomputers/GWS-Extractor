version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:80"
    environment:
      - VITE_CONVEX_URL=${VITE_CONVEX_URL}
      - VITE_GOOGLE_CLIENT_ID=${VITE_GOOGLE_CLIENT_ID}
    restart: unless-stopped
